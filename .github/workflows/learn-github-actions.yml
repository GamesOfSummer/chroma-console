- 
name: "Code Cov"

on:
  push:
    branches: [ main ]
  pull_request:
    # The branches below must be a subset of the branches above
    branches: [ main ]




name: Upload coverage reports to Codecov
  uses: codecov/codecov-action@v3
  env:
      CODECOV_TOKEN: ${{ dc8842ac-5aa3-4db8-9ab6-7b65e945c5e4 }}
